div.row-fluid
    div.topbar-gap.span24 &nbsp
    //div.span11.offset1
        div.alert.alert-success.alert-block.tutorial-success(data-intro="don't worry about connecting anything fancy, this is just to get you started", data-position="right", ng-show="isTutorialOn")
            button.close(type="button", data-dismiss="alert") &times;
            p Welcome, time to create your first connection. &nbsp
            p(ng-hide='isUserLoggedIn') If you want to create a connection, don't forget to &nbsp
                a(href="/login/") login.
            p(ng-show='isUserLoggedIn') Ok, you're all logged up.
            p In Knownodes we connect things and take careful attention to describe how they are connected, so the connection will make sense to other people.
            p Let's start by connecting something to "{{concept.title}}".
                p
                    a.tutorial-toggle(onclick="$('body').chardinJs('toggle')") Click this to see how
        div.triplet.span12
            div(triplet-input)

    div(ng-repeat="triplet in knownodeList | orderBy:orderProp", ng-switch on="triplet.otherResource.id == triplet.connection.fromNodeId")
        div.span12(ng-switch-when="true")
            div.triplet.span12 test
                div.span12(start-resource)
                div.span12(connection)
                div.span12(end-resource)
                p.span12
                    em {{(triplet.connection.__CreatedOn__) | timeAgo}} by {{triplet.connection.creator.firstName}} {{triplet.connection.creator.lastName}}
                    span
                        a.pull-right(ng-href="/edge/:{{triplet.connection.KN_ID}}", ng-switch on="triplet.commentCount")
                            span.text-warning(ng-switch-default) {{triplet.commentCount}} comments
                            span(ng-switch-when="0") comment
                //div.ui-icon.ui-icon-trash(style="float:right", ng-click="deleteArticle(triplet.otherResource.id, $index)", ng-show="checkOwnership(triplet.otherResource.creator.id)")
                //div(ng-show="checkOwnership(triplet.otherResource.user.id)", style="float:right")
                  a(class="ui-icon ui-icon-pencil", ng-href="", ng-show="false")
            div test
        div.span12(ng-switch-default)
            div.triplet.span12
                div.span12(end-resource)
                div.span12(connection)
                div.span12(start-resource)
                p.span12
                    em {{(triplet.connection.__CreatedOn__) | timeAgo}} by {{triplet.connection.creator.firstName}} {{triplet.connection.creator.lastName}}
                    span
                        a.pull-right(ng-href="/edge/:{{triplet.connection.KN_ID}}", ng-switch on="triplet.commentCount")
                            span.text-warning(ng-switch-default) {{triplet.commentCount}} comments
                            span(ng-switch-when="0") comment
                //div(class="ui-icon ui-icon-trash", style="float:right", ng-click="deleteArticle(triplet.article.id, $index)", ng-show="checkOwnership(triplet.article.user.id)")
                //div(ng-show="checkOwnership(triplet.article.user.id)", style="float:right")
                    a.ui-icon.ui-icon-pencil(ng-href="", ng-show="false")
            div
    div.span9(ng-repeat="link in concept.wikipediaLinks", ng-switch on="link.title != concept.title")
        div.triplet.span9(ng-switch-when="true")
            div.resource.span9#content
                a(ng-href="/wikipedia/{{concept.title}}", title="{{concept.title}}") {{concept.title}}
                    a(ng-href="http://en.wikipedia.org/wiki/{{concept.title}}", target="_blank")
                        img.titleIcon(src="/img/openInNewWindow.png", title="{{concept.title}}", width="20px", ng-show="concept.title")

            div.span9(class="auto-generated")
                div.span9
                    span &nbsp;Wikipedia Link
            div.resource.span9#content
                a(ng-href="/wikipedia/{{link.title}}", title="{{link.title}}") {{link.title}}
                    a(ng-href="http://en.wikipedia.org/wiki/{{link.title}}", target="_blank")
                        img.titleIcon(src="/img/openInNewWindow.png", title="{{link.title}}", width="20px", ng-show="link.title")
