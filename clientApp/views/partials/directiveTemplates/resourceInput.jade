div.resource-title.simplebox(data-intro='This is the resource "{{concept.title}}". Yep.', data-position="right" )
    span.start-resource-search-box(ng-show="resource == null")
        search-box
    span#content(ng-show="resource != null")
        {{resource.title}}
        button(ng-click="clear()") Clear


          //(data-intro="Using this box you can paste a link, write a thought or search for something to connect to '{{concept.title}}'", data-position="right"