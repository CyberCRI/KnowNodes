div
    div.popover.top.input-indicator
        div.popover-content(style="text-align:center") Create a connection.
          a.tutorial-toggle(onclick="$('body').chardinJs('toggle')") Show instructions
        div.arrow
    div.input-triplet
        div(start-resource-input)
        div(connection-input)
        div(end-resource-input)
        div.text-center.input-link-type
            p.text-center.input-link-type-text Why did you make this connection?

            div.btn-group.input-link-type-buttons(data-toggle="buttons-radio")
                button.btn.btn-primary.explain-button(type="button", ng-click="categoryClick('explain')") to explain
                button.btn.btn-primary.inspire-button(type="button", ng-click="categoryClick('inspire')") to inspire
                button.btn.btn-primary.critique-button(type="button", ng-click="categoryClick('critique')") to critique

            div
                button.btn.btn-primary(ng-click="submit(); tutorialOff();", ng-disabled="submitted") Submit
                span.alert(ng-show="submitErrorMessage") {{submitErrorMessage}}
