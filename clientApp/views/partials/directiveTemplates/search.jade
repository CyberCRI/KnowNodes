div
  input.search-query(type="text", ng-model="keyword", placeholder="Search")
  a.btn.btn-inverse(ng-click="searchByKeyword()") submit
  h5 search results
  div(ng-repeat="item in searchResults")
    a.btn.btn-link(ng-click="addAsNode(item.results.KN_ID)") {{item.results.title}}

