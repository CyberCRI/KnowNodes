div.topbar
  a(href='#')
    img.topbar-logo(src="/img/knownodes-logo.png")
  span.topbar-search(search-box)
  span
      a.btn.btn-link(ng-href="/map/{{resourceId}}", ng-show="mapButton") Map mode
      a.btn.btn-link(ng-href="/article/{{resourceId}}", ng-show="resourceButton") List mode
      span(top-bar-login)
