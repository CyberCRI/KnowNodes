//
   Created with JetBrains WebStorm.
   User: admin
   Date: 16/01/13
   Time: 00:20
   To change this template use File | Settings | File Templates.
//TODO: nodify the list to be made of title: "m2 course"; and than all the concepts as part of the course.
#subjectList
div.subheader
  div.span23.offset1
    .subheader(subtitle)
    span.btn.btn-link.sort sort:
    div.btn-group(data-toggle="buttons-radio")
      button.btn.btn-link.active-red(type="button", ng-click="orderProp='date'") New
      button.btn.btn-link(type="button", ng-click="orderProp='votes'") Popular
      button.btn.btn-link(type="button", ng-click="setMaster(subtitle)") Hot
  div(ng-repeat="edge in edges | orderBy:orderProp").span11.offset1
    include knownodeR
//div
    a(href="/concept/relatedTo/{{concept.conceptId}}") {{concept.title}}
      div
          {{concept.content}}

//#subjectList
//    h2 {{concept.title}}
//    p(class='well') {{concept.bodyText}}
//    h4 Related Knownodes to {{concept.title}}:
//    ul(ng-show='knownodeList.length > 0')
//        li(ng-repeat="knownode in knownodeList")
//            div
//                b
//                    a(href="/subject/{{kn.KN_ID}}") {{kn.title}}
//            div
//                {{subject.bodyText}}
//    p(ng-show='knownodeList.length == 0') No knownodes yet :(
//    a(class='btn btn-success', href='/addPostURL/{{conceptId}}') Add a new article knownode